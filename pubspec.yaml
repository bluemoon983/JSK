name: jck
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - image/splash/다운로드.png # 스플래시 로고 추가

flutter_native_splash:
  color: "#000000"     # 배경색
  image: image/splash/다운로드.png  # 로고 이미지 경로
  android: true
  ios: true
